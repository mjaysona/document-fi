export enum AccessType {
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum AccessTypeLabel {
  create = 'Create',
  read = 'View',
  update = 'Update',
  delete = 'Delete',
}
